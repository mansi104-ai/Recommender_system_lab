Run `python assi_1/solve_assignment_1.py` from the repository root.

The script uses `assi_4/ratings_small.csv` as input and generates:
- cleaned / feature-engineered CSV files
- group-wise summary tables
- head vs tail item classification
- EDA charts
- `REPORT.md` and `summary.txt`
